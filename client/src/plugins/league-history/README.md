# League History (client plugin)

Titles and Season Explorer champion cards both use `league_medals.championOwner` (same string); dynasty counts match `displayName` on `leagueHistoryStandings` owners.
