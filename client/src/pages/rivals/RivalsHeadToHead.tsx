/** Canonical `/rivals/head-to-head` — Head-to-Head Ledger (matrix + series records). */
import { RivalryCenter } from "@/pages/RivalryCenter";

export function RivalsHeadToHead() {
  return <RivalryCenter variant="head-to-head" />;
}
