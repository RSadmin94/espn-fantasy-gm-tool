# extension (bundle snapshot)

The source tree may include a folder **`ext_v177`** for a packaged extension build. If it was not present when this bundle was generated, nothing was copied here—use the live repo’s `chrome-extension/` (or documented release artifact) instead.
